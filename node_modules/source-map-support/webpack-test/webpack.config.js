var webpack = require('webpack');

module.exports = {
  entry: './script.js',
  devtool: 'inline-source-map',
  output: {
    filename: 'compiled.js'
  },
  resolve: {
    extensions: ['', '.js']
  },

  module: {
      loaders: [
          {
              test: /\.css$/,
              exclude: /node_modules/,
              loader: 'style-loader!css-loader?sourceMap!autoprefixer-loader?browsers=Last 2 versions'
          }
      ]
  }
};
